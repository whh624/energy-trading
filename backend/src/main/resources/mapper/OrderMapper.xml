<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.energytrading.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.energytrading.entity.Order">
        <id column="id" property="id"/>
        <result column="order_id_on_chain" property="orderIdOnChain"/>
        <result column="seller_address" property="sellerAddress"/>
        <result column="seller_name" property="sellerName"/>
        <result column="seller_trust_score" property="sellerTrustScore"/>
        <result column="amount" property="amount"/>
        <result column="price" property="price"/>
        <result column="status" property="status"/>
        <result column="created_time" property="createdTime"/>
        <result column="updated_time" property="updatedTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        o.id, o.order_id_on_chain, o.seller_address, u.username as seller_name, u.trust_score as seller_trust_score, o.amount, o.price, o.status, o.created_time, o.updated_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM energy_orders o
        LEFT JOIN user u ON LOWER(o.seller_address) = LOWER(u.blockchain_address)
        WHERE o.id = #{id}
    </select>

    <select id="selectByOrderIdOnChain" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM energy_orders o
        LEFT JOIN user u ON LOWER(o.seller_address) = LOWER(u.blockchain_address)
        WHERE o.order_id_on_chain = #{orderIdOnChain}
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM energy_orders o
        LEFT JOIN user u ON LOWER(o.seller_address) = LOWER(u.blockchain_address)
        WHERE o.status = #{status}
        ORDER BY o.created_time DESC
    </select>

    <select id="selectBySellerAddress" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM energy_orders o
        LEFT JOIN user u ON LOWER(o.seller_address) = LOWER(u.blockchain_address)
        WHERE o.seller_address = #{sellerAddress}
        ORDER BY o.created_time DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM energy_orders o
        LEFT JOIN user u ON LOWER(o.seller_address) = LOWER(u.blockchain_address)
        ORDER BY o.created_time DESC
    </select>

    <insert id="insert" parameterType="com.energytrading.entity.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO energy_orders (order_id_on_chain, seller_address, amount, price, status, is_abnormal, created_time, updated_time)
        VALUES (#{orderIdOnChain}, #{sellerAddress}, #{amount}, #{price}, #{status}, #{isAbnormal}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.energytrading.entity.Order">
        UPDATE energy_orders
        SET order_id_on_chain = #{orderIdOnChain},
            seller_address = #{sellerAddress},
            amount = #{amount},
            price = #{price},
            status = #{status},
            is_abnormal = #{isAbnormal},
            updated_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE energy_orders
        SET status = #{status},
            updated_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateAmount">
        UPDATE energy_orders
        SET amount = #{amount},
            updated_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 新增：获取所有开放订单的平均价格 -->
    <select id="selectAveragePrice" resultType="java.lang.Double">
        SELECT AVG(price) FROM energy_orders WHERE status = 0
    </select>

    <!-- 新增：更新订单的异常状态 -->
    <update id="updateIsAbnormal">
        UPDATE energy_orders
        SET is_abnormal = #{isAbnormal},
            updated_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM energy_orders WHERE id = #{id}
    </delete>

</mapper>
