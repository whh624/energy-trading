# P2P电力交易系统 - 完整测试教程 (V2.0 算法增强版)

## 📋 测试环境准备

### 系统服务状态
✅ **Ganache区块链**：运行中 (http://127.0.0.1:8545) - **建议保留 10 个测试账户**  
✅ **MySQL数据库**：运行中 (localhost:3306)  
✅ **后端服务**：运行中 (http://localhost:8080)  
✅ **前端服务**：运行中 (http://localhost:3000)

### 测试工具准备
- **MetaMask扩展**：已连接至本地 Ganache 网络
- **Postman (可选)**：用于直接调用算法 API 演示

---

## 🎯 核心功能测试流程

### 第一步：账户与钱包准备
1. 启动 Ganache，确保前 3 个账户有充足的 ETH。
2. 在 MetaMask 中导入这 3 个账户，分别作为：**管理员/验证者**、**卖家**、**买家**。

### 第二步：PBFT 共识算法测试 (核心亮点)
**测试目标**：验证交易是否必须经过链上三阶段共识才能成功。

1. **发起购买**：以“买家”身份在交易市场选择一个订单点击“购买”。
2. **观察后端/控制台**：
   - 应该看到 `[Step 1] 发起 Pre-prepare 交易`。
   - 应该看到 `[Step 2] 正在收集 Prepare 投票 (2f+1)`。
   - 应该看到 `[Step 3] 正在收集 Commit 投票 (2f+1)`。
3. **验证结果**：
   - 打开 Ganache 界面，你会发现产生了连续的 7 笔交易（1次预准备 + 3次准备 + 3次提交）。
   - 只有这 7 笔交易成功后，最后的 `buyEnergy` 业务交易才会出现在链上。

### 第三步：Dijkstra 电力调度算法测试
**测试目标**：验证电力溯源是否基于图论算法动态生成最优路径。

1. **进入溯源页面**：点击侧边栏的“交易溯源”。
2. **输入交易哈希**：输入刚才购买成功的交易 Hash。
3. **查看溯源路径**：
   - 预期结果：路径不再是固定的，而是包含了 `110kV 变电站`、`微电网控制器` 等节点。
   - 验证：这些节点是 `AlgorithmService` 根据卖家和买家的逻辑位置，利用 Dijkstra 算法计算出的**损耗最小路径**。

### 第四步：PoC 贡献度模型演示
**测试目标**：验证节点权重分配逻辑。

1. **使用 API 演示**：访问 `http://localhost:8080/api/algorithm/consensus-demo`。
2. **查看 JSON 返回值**：
   - 观察 `logs` 字段。
   - 验证：系统展示了不同节点的贡献度（如 `StateGrid` 得分最高），这证明了共识过程中存在非对称权重的逻辑。

---

## 📝 基础业务测试步骤 (常规)

### 1. 注册与登录
- 访问 `http://localhost:3000`。
- 注册新用户，检查数据库 `user` 表是否新增记录。

### 2. 发布电力挂单
- 进入“发布挂单”页面。
- 输入电量（如 200 kWh）和价格。
- 确认 MetaMask 弹出窗口。
- 检查“交易市场”是否实时更新。

### 3. 市场撮合优化测试
- 访问 `http://localhost:8080/api/algorithm/optimize-matching`。
- 验证：系统是否返回了基于价格和碳排放指数优化后的匹配方案。

---

## 🔍 异常案例测试 (QA)

| 测试场景 | 预期行为 | 验证状态 |
| :--- | :--- | :--- |
| **共识投票不足** | 修改代码减少投票次数，交易应直接回滚，Ganache 无业务记录 | ✅ 通过 |
| **余额不足购买** | 购买超出钱包余额的电力，MetaMask 应报错或交易失败 | ✅ 通过 |
| **非法节点投票** | 使用非验证者地址调用 `voteForOrder`，合约应报错 | ✅ 通过 |

---

## 💡 答辩演示小贴士
1. **重点展示 Ganache 日志**：向老师展示 PBFT 的多笔交易，证明这是“真共识”而非“假模拟”。
2. **重点展示溯源图**：强调图中的节点是通过 Dijkstra 算法动态生成的，体现电力系统的专业性。
